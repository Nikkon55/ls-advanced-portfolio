.scroll__icon{
    width: 50px;
    height: 50px;
    background: svg-load('scroll.svg', fill=$text-color, width=100%, height=100%);
    margin: 0 auto;
    position: absolute;
    bottom: 10px;
    left: 50%;
    transform: translateX(-50%);
}