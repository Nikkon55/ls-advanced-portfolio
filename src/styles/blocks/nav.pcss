.nav__list {
    display: flex;
    flex-direction: row;
}
.nav__item {
    margin-right: 60px;
    &:last-child {
        margin-right: 0px;
    }
}

.nav__link {
    text-decoration: none;
    &:hover {
        text-decoration: underline;
    }
}