.tags {
    display: flex;
    flex-direction: row;
}
.tags__item {
    display: block;
    font-size: 15px;
    opacity: 0.7;
    background-color: #f3f5f9;
    border-radius: 23px;
    padding: 20px;
    margin-right: 10px;

    &:last-child {
        margin-right: 0;
    }
}